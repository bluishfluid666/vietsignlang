pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

include ':app'
include ':OpenCV'
project(':OpenCV').projectDir = new File(rootDir, 'OpenCV-android-sdk/sdk/')
